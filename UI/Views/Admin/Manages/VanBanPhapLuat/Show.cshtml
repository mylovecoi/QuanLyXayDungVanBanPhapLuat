@model DataAccess.Entities.Manages.AttachedFile

<div class="row">
    <div class="col-xl-4">
        <div class="form-group fv-plugins-icon-container">
            <label style="font-weight:bold; color:blue">Số văn bản: </label>
            <label class="font-weight-bold">@Model.SoVanBan</label>
        </div>
    </div>
    <div class="col-xl-4">
        <div class="form-group fv-plugins-icon-container">
            <label style="font-weight:bold; color:blue">Ngày ban hành: </label>
            <span class="font-weight-bold">@FuntionGlobal.ConvertDateToStr(Model.NgayBanHanh)</span>
        </div>
    </div>
    <div class="col-xl-4">
        <div class="form-group fv-plugins-icon-container">
            <label style="font-weight:bold; color:blue">Ngày áp dụng: </label>
            <span class="font-weight-bold">@FuntionGlobal.ConvertDateToStr(Model.NgayApDung)</span>
        </div>
    </div>
    <div class="col-xl-12">
        <div class="form-group fv-plugins-icon-container">
            <label style="font-weight:bold; color:blue">Mô tả: </label>
            <span class="font-weight-bold">@Model.MoTa</span>
        </div>
    </div>
    <div class="col-xl-12">
        <div class="form-group fv-plugins-icon-container">
            <label style="font-weight:bold; color:blue">Đường dẫn: </label>
            <span class="font-weight-bold">@Model.Url</span>
        </div>
    </div>
    @if (!string.IsNullOrEmpty(Model.FileName))
    {
        <div class="col-xl-12">
            <div class="form-group fv-plugins-icon-container">
                <label style="font-weight:bold; color:blue">
                    File Upload:
                    <a href="/Manages/Download/AttachedFile?id=@Model.Id" target="_blank" ; font-weight: bold;
                       color: red; text-decoration: underline;">
                        📎 File hiện tại (@Model.FileName)
                    </a>
                </label>
            </div>
        </div>
    }
</div>
