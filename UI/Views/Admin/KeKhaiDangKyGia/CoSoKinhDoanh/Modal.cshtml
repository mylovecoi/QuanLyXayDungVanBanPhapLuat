<!--Create-Modal-->
<div class="modal fade" id="Create_Modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="card-label">
                    Cơ sở kinh doanh @ViewData["Title"]
                    <span class="d-block text-muted pt-2 font-size-sm">Thêm mới Cơ sở kinh doanh @ViewData["Title"]</span>
                </h3>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <i aria-hidden="true" class="ki ki-close"></i>
                </button>
            </div>
            <div class="modal-body">
                <form id="frm_create"></form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light-danger font-weight-bold" data-dismiss="modal">Đóng</button>
                <button type="button" class="btn btn-primary font-weight-bold" onclick="Store()">Thêm mới</button>
            </div>
        </div>
    </div>
</div>

<!--Edit Modal-->
<div class="modal fade" id="Edit_Modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="card-label">
                    @ViewData["Title"]
                    <span class="d-block text-muted pt-2 font-size-sm">Chỉnh sửa @ViewData["Title"]</span>
                </h3>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <i aria-hidden="true" class="ki ki-close"></i>
                </button>
            </div>
            <div class="modal-body">
                <form id="frm_edit"></form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light-danger font-weight-bold" data-dismiss="modal">Đóng</button>
                <button type="button" class="btn btn-primary font-weight-bold" onclick="Update()">Cập nhật</button>
            </div>
        </div>
    </div>
</div>

<!--Delete Modal-->
<div class="modal fade" id="Delete_Modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="card-label">
                    @ViewData["Title"]
                    <span class="d-block text-muted pt-2 font-size-sm">Xóa @ViewData["Title"]</span>
                </h3>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <i aria-hidden="true" class="ki ki-close"></i>
                </button>
            </div>
            <form asp-controller="KeKhaiDangKyGiaCsKd" asp-action="Delete" method="post">
                <div class="modal-body">
                    <div class="form-group fv-plugins-icon-container">
                        <label>
                            Tên cơ sở kinh doanh:&nbsp;<b id="ten_delete" name="ten_delete" style="color: #0000FF"></b>
                        </label>
                    </div>
                    <input hidden id="id_delete" name="id_delete" />
                    <input hidden id="manghe_delete" name="MaNghe" value="@ViewData["MaNghe"]" />
                    @Html.AntiForgeryToken()
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-light-primary font-weight-bold" data-dismiss="modal">Đóng</button>
                    <button type="submit" class="btn btn-danger font-weight-bold">Đồng ý</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="~/js/modal.validation.js"></script>
<script>
    function Create(maNghe) {
        const csrfToken = $('input[name="__RequestVerificationToken"]').val();
        $.ajax({
            url: '/KeKhaiDangKyGia/CoSoKinhDoanh/Create',
            type: 'POST',
            headers: {
                'RequestVerificationToken': csrfToken
            },
            data: {
                MaNghe: maNghe
            },
            success: function (data) {
                $('#frm_create').html(data);
                $('#Create_Modal').modal('show');
                $('#frm_create').find('.select2multi').select2({ width: '100%' });
            },
            error: function (xhr) {
                toastr.error(xhr.responseText || "Không thể tải dữ liệu!", "Lỗi!");
            }
        })
    }

    function Store() {
        var form = document.getElementById('frm_create');
        if (!form) {
            toastr.error("Không tìm thấy form với id 'frm_create'", "Lỗi!");
            return;
        }
        const fieldsToValidate = [
            { id: 'DoanhNghiepQuanLyId', message: 'Doanh nghiệp quản lý không được bỏ trống' },
            { id: 'TenCsKd', message: 'Tên cơ sở kinh doanh không được bỏ trống' },
            { id: 'DiaChi', message: 'Địa chỉ không được bỏ trống' },
            { id: 'SoDienThoai', message: 'Số điện thoại không được bỏ trống' }
        ];
        if (validateModalFormFields(form, fieldsToValidate)) {
            const csrfToken = $('input[name="__RequestVerificationToken"]').val();
            const formData = new FormData(form);
            $.ajax({
                url: '/KeKhaiDangKyGia/CoSoKinhDoanh/Store',
                type: 'POST',
                headers: {
                    'RequestVerificationToken': csrfToken
                },
                data: formData,
                processData: false,       
                contentType: false,   
                dataType: 'JSON',
                success: function (data) {
                    if (data.status == 'success') {
                        toastr.success(data.message);
                        location.reload();
                    } else {
                        toastr.error(data.message, "Lỗi!");
                    }
                }
            })
        }
    }

    function Edit(id_edit) {
        const csrfToken = $('input[name="__RequestVerificationToken"]').val();
        $.ajax({
            url: '/KeKhaiDangKyGia/CoSoKinhDoanh/Edit',
            type: 'POST',
            headers: {
                'RequestVerificationToken': csrfToken
            },
            data: {
                Id: id_edit,
            },
            success: function (data) {
                $('#frm_edit').html(data);
                $('#Edit_Modal').modal('show');
                $('#frm_edit').find('.select2multi').select2({ width: '100%' });
            },
            error: function (xhr) {
                toastr.error(xhr.responseText || "Không thể tải dữ liệu!", "Lỗi!");
            }
        })
    }

    function Update() {
        var form = document.getElementById('frm_edit');
        if (!form) {
            toastr.error("Không tìm thấy form với id 'frm_edit'", "Lỗi!");
            return;
        }
        const fieldsToValidate = [
            { id: 'DoanhNghiepQuanLyId', message: 'Doanh nghiệp quản lý không được bỏ trống' },
            { id: 'TenCsKd', message: 'Tên cơ sở kinh doanh không được bỏ trống' },
            { id: 'DiaChi', message: 'Địa chỉ không được bỏ trống' },
            { id: 'SoDienThoai', message: 'Số điện thoại không được bỏ trống' }
        ];
        if (validateModalFormFields(form, fieldsToValidate)) {
            const csrfToken = $('input[name="__RequestVerificationToken"]').val();
            const formData = new FormData(form);
            $.ajax({
                url: '/KeKhaiDangKyGia/CoSoKinhDoanh/Update',
                type: 'POST',
                headers: {
                    'RequestVerificationToken': csrfToken
                },
                data: formData,
                processData: false,
                contentType: false,
                dataType: 'JSON',
                success: function (data) {
                    if (data.status == 'success') {
                        toastr.success(data.message);
                        location.reload();
                    } else {
                        toastr.error(data.message, "Lỗi!");
                    }
                }
            })
        }
    }

    function Delete(id, name) {
        document.getElementById("id_delete").value = id;
        document.getElementById("ten_delete").innerHTML = name;
        $('#Delete_Modal').modal('show');
    }
</script>
