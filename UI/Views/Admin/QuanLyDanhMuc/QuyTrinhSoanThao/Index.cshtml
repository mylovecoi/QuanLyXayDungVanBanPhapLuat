@using Services.QuanLyDanhMuc
@using UI.ViewModels

@{
    int pageSize = (ViewData["PageInfo"] as VMPageInfo)?.PageSize ?? 5;
    string timKiem = (ViewData["PageInfo"] as VMPageInfo)?.Search ?? "";
}

@model IEnumerable<QuyTrinhSoanThaoListItemModel>

@section custom_script {
    <script>
        function ClearTxtSearch() {
            $('#txt_Search').val('');
        }

        function SubmitTxtSearch() {
            ReloadFrm();
        }

        $('#PageSize_Search').change(function () {
            ReloadFrm();
        });

        function ReloadFrm() {
            var currentPathUrl = window.location.pathname + '?';
            var timKiem = '&timKiem=' + $('#txt_Search').val();
            var pageSize = '&pageSize=' + $('#PageSize_Search').val();
            var url = currentPathUrl + timKiem + pageSize;
            window.location.href = url;
        }

        function ClickPage(pageSelect) {
            var currentPathUrl = window.location.pathname + '?';
            var timKiem = '&timKiem=' + $('#txt_Search').val();
            var pageSize = '&pageSize=' + $('#PageSize_Search').val();
            var pageCurrent = '&pageCurrent=' + pageSelect;
            var url = currentPathUrl + timKiem + pageSize + pageCurrent;
            window.location.href = url;
        }
    </script>
}

<div class="card card-custom">
    <div class="card-header flex-wrap border-0 pt-6 pb-0">
        <div class="card-title">
            <h3 class="card-label">
                <b>@ViewData["Title"]?.ToString()?.ToUpper()</b>
                <span class="d-block text-muted pt-2 font-size-sm">
                    Quản lý @FuntionGlobal.CapitalizeAll(ViewData["Title"]?.ToString())
                </span>
            </h3>
        </div>
        <div class="card-toolbar">
            <a asp-controller="QuyTrinhSoanThao" asp-action="Create" class="btn btn-primary font-weight-bolder">
                <i class="la la-plus"></i>Thêm mới
            </a>
        </div>
    </div>
    @Html.AntiForgeryToken()
    <div class="card-body pt-0">
        <hr />
        <div class="row">
            <div class="col-xl-3" style="text-align:left">
                <div class="form-group fv-plugins-icon-container">
                    <label class="text-blue">Hiển thị</label>
                    <select class="form-control" name="PageSize_Search" id="PageSize_Search">
                        <!option value="5" @(pageSize == 5 ? "selected" : "")>5 thông tin</!option>
                        <!option value="10" @(pageSize == 10 ? "selected" : "")>10 thông tin</!option>
                        <!option value="20" @(pageSize == 20 ? "selected" : "")>20 thông tin</!option>
                        <!option value="50" @(pageSize == 50 ? "selected" : "")>50 thông tin</!option>
                        <!option value="100" @(pageSize == 100 ? "selected" : "")>100 thông tin</!option>
                    </select>
                </div>
            </div>
            <div class="col-xl-9" style="text-align:left">
                <div class="form-group fv-plugins-icon-container">
                    <label class="text-blue">Tìm kiếm</label>
                    <div class="input-group">
                        <input class="form-control" name="txt_Search" id="txt_Search" placeholder="Tìm kiếm..." value="@timKiem">
                        <div class="input-group-prepend">
                            <button class="btn btn-danger" type="button" onclick="ClearTxtSearch()"><i class="las la-times-circle"></i></button>
                        </div>
                        <div class="input-group-append">
                            <button class="btn btn-primary" type="button" onclick="SubmitTxtSearch()"><i class="las la-search"></i></button>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="table-responsive border rounded-lg mb-8">
            <table class="table table-striped table-hover table-responsive-lg rounded-lg m-0">
                <thead class="thead-light">
                    <tr style="text-align:center;" class="table-active">
                        <th>Mã quy trình</th>
                        <th>Tên quy trình</th>
                        <th>Cấp</th>
                        <th>Loại văn bản</th>
                        <th>Phiên bản</th>
                        <th>Số bước</th>
                        <th>Số nhánh</th>
                        <th>Theo dõi</th>
                        <th width="12%">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="align-middle font-weight-bold">@item.MaQuyTrinh</td>
                                <td class="align-middle">@item.TenQuyTrinh</td>
                                <td class="text-center align-middle">@(string.IsNullOrWhiteSpace(item.CapApDung) ? "-" : item.CapApDung)</td>
                                <td class="align-middle">@(string.IsNullOrWhiteSpace(item.TenLoaiVanBan) ? "-" : item.TenLoaiVanBan)</td>
                                <td class="text-center align-middle">@item.PhienBan</td>
                                <td class="text-center align-middle">@item.SoBuoc</td>
                                <td class="text-center align-middle">@item.SoNhanhChuyen</td>
                                <td class="text-center align-middle">
                                    @if (item.TrangThai)
                                    {
                                        <span class="label label-lg label-light-success label-inline">Kích hoạt</span>
                                    }
                                    else
                                    {
                                        <span class="label label-lg label-light-secondary label-inline">Ngừng sử dụng</span>
                                    }
                                </td>
                                <td class="text-center align-middle">
                                    <a href="/QuanLyDanhMuc/QuyTrinhSoanThao/Edit/@item.Id" class="btn btn-sm btn-clean btn-icon" title="Xem chi tiết / Chỉnh sửa">
                                        <i class="icon-lg la la-edit text-success"></i>
                                    </a>
                                    <button type="button" class="btn btn-sm btn-clean btn-icon" title="Xóa" onclick="Delete('@item.Id', '@item.TenQuyTrinh')">
                                        <i class="icon-lg la la-trash text-danger"></i>
                                    </button>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td style="text-align:center" colspan="9">Không tìm thấy thông tin.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @await Html.PartialAsync("~/Views/Shared/Pagination.cshtml")
    </div>
</div>

@await Html.PartialAsync("~/Views/Admin/QuanLyDanhMuc/QuyTrinhSoanThao/Modal.cshtml")
